function num(x) {
    if (x % 5 == 0 && x % 3 == 0) {
        return 'FizzBuzz'
    }
    if (x % 3 == 0) 
        return 'Fizz'

     if (x % 5 == 0) 
        return 'Buzz'
     if (x == !isNaN)
    return x;
    else (x % 5 !== 0 && x % 3 !== 0)
    return x;
}
console.log('a', num('a'))
for (let i = 0; i <= 100; i++) {

console.log(i, num(i))}