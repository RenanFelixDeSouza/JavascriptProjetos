let rand = Math.floor(Math.random() * (20 - 1)) + 1;
while (rand !== 10) {
    const aleatorio = Math.floor(Math.random() * (20 - 1)) + 1;
    let rand = aleatorio
    console.log(aleatorio);
}

//const min = 1;
//const max = 50;
//let rand = aleatorio(min, max);

//while (rand !== 10) {
  //  rand = aleatorio(min, max);
 //   console.log(rand);
//}
