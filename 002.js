const frutas = ['maça', 'pera', 'uva', 'banana', 'melão', 'Jabuticaba'];




for (let i = 0; i < frutas.length; i++) {
   // const par = i% 2 ===0 ? 'par' : 'impar';
   // console.log(`Número ${i} é ${par}`);
   console.log(frutas[i]);

}